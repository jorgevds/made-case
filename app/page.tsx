import { Hero } from "@/components/index";

export default function Home() {
    return (
        <main className="flex flex-col min-h-screen flex-grow items-center justify-between ">
            <Hero />
        </main>
    );
}
