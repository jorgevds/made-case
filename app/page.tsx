import { Hero } from "@/components/index";

export default function Home() {
    return (
        <main className="flex flex-col min-h-0 flex-grow items-center justify-between ">
            <Hero />
        </main>
    );
}
